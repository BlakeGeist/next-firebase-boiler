import React from "react";
import Nav from "./Nav";

const Header = () => {
    return (
        <div>
            <div className="container">
                <Nav />
            </div>
        </div>
    );
};

export default Header;
